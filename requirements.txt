# Essential dependencies for StarDust Motion Capture System
# Python version requirement (tested with Python 3.12.2)

# Core scientific computing
numpy>=1.21.0

# Computer vision and media processing
opencv-python>=4.5.0
mediapipe>=0.8.0

# 3D processing and visualization
matplotlib>=3.5.0

# System Dependencies (must be installed separately):
# - Blender 3.0+ (sole tool used for USDZ to OBJ conversion)
#   macOS: Download from https://www.blender.org/download/
#
# - FFmpeg (for video processing)
#   macOS: brew install ffmpeg

# Installation Notes:
# 1. Install Python 3.12.2 (tested version)
# 2. Install Blender and FFmpeg as system dependencies
# 3. Install Python packages: pip install -r requirements.txt